@inject UserManager<UserModel> _userManager

@if (Context.User.Identity.IsAuthenticated)
{
    var activeUser = await _userManager.GetUserAsync(Context.User);

    <span class="nav-link text-info">Olá
        <b>@StringExtensions.GetFirstWord(activeUser.FullName)</b>!</span>
    <a class="nav-link pe-0" asp-action="Logout" 
        asp-controller="Employee">Sair</a>
}
else
{
    <a class="nav-link " asp-action="Login" 
        asp-controller="Employee">Entrar</a>
}